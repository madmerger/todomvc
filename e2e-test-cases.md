# E2Eテストケース (JavaScript ES6 TodoMVC)

`app-spec.md` の「Functionality」要件をもとにしたブラウザーE2Eテストケース。
対象アプリ: `examples/javascript-es6`（UIは日本語）。

## 前提

- `cd examples/javascript-es6 && npm install`
- `npm run build && npm run serve` → `http://localhost:7002`
- 本実装のストアはメモリー内（`src/store.js`）のため、リロードでデータは消える。TC-10はこの前提で仕様との差分を確認するケース。

| 記号 | 意味 |
| --- | --- |
| 入力欄 | `.new-todo`（プレースホルダー「何をする必要がありますか？」） |
| 一括トグル | 入力欄左の「すべて完了にする」矢印 `.toggle-all` |
| フィルター | 「すべて」`#/` / 「未完了」`#/active` / 「完了」`#/completed` |
| 削除ボタン | 行ホバーで表示される `×`（`.destroy`） |

---

## TC-01 タスクが0件のときの表示

1. `http://localhost:7002` を開く。

期待結果:
- `.main`（リスト・一括トグル）と `.footer`（残件数・フィルター・完了したタスクを削除）が表示されない。
- 入力欄にフォーカスが当たっている（`autofocus`）。

## TC-02 タスクの追加

1. 入力欄に「牛乳を買う」と入力しEnter。
2. 続けて「本を読む」「散歩する」を追加。

期待結果:
- 各Enterでリスト末尾に追加され、入力欄がクリアされる。
- 表示順は追加順（牛乳を買う → 本を読む → 散歩する）。
- 残件数が「残り 3 件」になる。

## TC-03 空文字・空白のみの入力は無視される

1. 入力欄で何も入力せずEnter。
2. 半角スペースのみを入力しEnter。
3. 「  余白あり  」（前後に空白）を入力しEnter。

期待結果:
- 1, 2 ではタスクが作成されない。
- 3 ではトリムされた「余白あり」が1件作成される。

## TC-04 個別タスクの完了・未完了

1. TC-02の3件がある状態で「本を読む」のチェックボックスをクリック。
2. もう一度クリックして戻す。

期待結果:
- チェック時: 行に取り消し線（`completed` クラス）が付き、残件数が「残り 2 件」。
- 解除時: 取り消し線が消え、残件数が「残り 3 件」。

## TC-05 残件数の単数・複数表記

1. タスクを1件だけ残す。

期待結果:
- 件数が `<strong>` で囲まれて表示される。
- 0件・1件・複数件いずれでも日本語表記「残り N 件」が崩れない。

## TC-06 すべて完了にする（一括トグル）

1. 3件ある状態で一括トグルをクリック。
2. 1件だけチェックを外す。
3. 再度すべてチェックする。

期待結果:
- 1: 全タスクが完了になり、残件数が「残り 0 件」。
- 2: 一括トグルの選択状態が解除される。
- 3: 一括トグルが再び選択状態になる。

## TC-07 タスクの編集

1. 「散歩する」のラベルをダブルクリック。
2. 「走る」に書き換えてEnter。
3. 別のタスクをダブルクリックし、内容を変えてから入力欄の外をクリック（blur）。
4. 別のタスクをダブルクリックし、内容を変えてからEscキー。
5. 別のタスクをダブルクリックし、すべて削除してEnter。

期待結果:
- 1: `editing` クラスが付き他のコントロールが隠れ、既存タイトル入りの入力にフォーカスが当たる。
- 2, 3: 編集内容が保存され `editing` が外れる。
- 4: 編集が破棄され元のタイトルのまま。
- 5: 空文字のためそのタスクが削除される。

## TC-08 タスクの削除

1. 任意の行にマウスを重ねる。
2. 表示された `×` をクリック。

期待結果:
- ホバー前は `×` が見えず、ホバーで表示される。
- クリックでその行だけが消え、残件数が1減る。

## TC-09 完了したタスクを削除

1. 全件未完了の状態にする。
2. 1件を完了にする。
3. 「完了したタスクを削除」をクリック。

期待結果:
- 1: ボタンが表示されない（完了0件のため）。
- 2: ボタンが表示される。
- 3: 完了タスクのみ削除され、一括トグルの選択状態も解除される。

## TC-10 フィルター（ルーティング）

1. 未完了2件・完了1件の状態を作る。
2. 「未完了」をクリック。
3. 「完了」をクリック。
4. 「すべて」をクリック。
5. `#/active` 表示中に、表示されているタスクのチェックボックスをクリック。

期待結果:
- 2: URLが `#/active`、未完了2件のみ表示、「未完了」に選択枠（`selected`）。
- 3: URLが `#/completed`、完了1件のみ表示。
- 4: URLが `#/`、全件表示。
- 5: 完了になった時点でそのタスクが一覧から消える。

## TC-11 永続化（仕様との差分確認）

1. 数件タスクを追加する。
2. ページを再読み込みする。

期待結果（`app-spec.md`）:
- タスクが保持され、選択中のフィルターも維持される。

実装の現状:
- ストアがメモリー内のためタスクは消える。**既知の仕様差分**として記録し、他ケースの実行中はリロードしない。
