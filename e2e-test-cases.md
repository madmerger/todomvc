# TodoMVC E2E テストケース（app-spec.md 準拠）

対象実装: `examples/javascript-es6`（日本語 UI）

## 前提・環境

- 起動: `cd examples/javascript-es6 && npm install && npm run build && npm run serve` → `http://localhost:7002`
- 保存はメモリー内実装（`src/store.js` の `memoryStorage`）。**ページを再読み込みするとデータは消える**ため、1 シナリオ中にリロードしない。
- UI 文言（日本語）
  - 入力欄プレースホルダー: 「何をする必要がありますか？」
  - 一括完了: 入力欄左の矢印（`.toggle-all-label`）
  - 残件数: 「残り **N** 件」（`.todo-count`）
  - フィルター: 「すべて」/「未完了」/「完了」（`#/`, `#/active`, `#/completed`）
  - 完了削除ボタン: 「完了したタスクを削除」（`.clear-completed`）

## TC-01 todo が 0 件のときの表示（No todos）

1. アプリを開く。
2. リスト・フッターの表示を確認する。

期待値: `.main` と `.footer` が非表示（残件数、フィルター、完了削除ボタンが見えない）。入力欄にフォーカスがある（autofocus）。

## TC-02 新規追加（New todo）

1. 入力欄に「牛乳を買う」と入力し Enter。
2. 続けて「郵便を出す」「部屋を片付ける」を追加。

期待値: 3 件が追加順（上から 牛乳を買う → 郵便を出す → 部屋を片付ける）で表示。追加ごとに入力欄が空になる。残件数「残り 3 件」。`.main`/`.footer` が表示される。

## TC-03 空文字・空白のみは追加されない（trim）

1. 入力欄で Enter のみを押す。
2. 半角スペースのみを入力して Enter。
3. 「  余白あり  」（前後に空白）を入力して Enter。

期待値: 1・2 では todo が増えない（件数は TC-02 の 3 件のまま）。3 では「余白あり」が前後の空白を除いたタイトルで追加される。

## TC-04 単一 todo の完了・未完了切り替え

1. 「牛乳を買う」行のチェックボックスをクリック。
2. 同じチェックボックスを再度クリック。

期待値: 1 でタイトルに取り消し線が付き（`li.completed`）、残件数が 1 減る。完了削除ボタンが表示される。2 で取り消し線が消え、残件数が戻り、完了が 0 件になると完了削除ボタンが非表示になる。

## TC-05 一括完了（Mark all as complete）

1. すべて未完了の状態で入力欄左の矢印をクリック。
2. 再度矢印をクリック。
3. 1 件だけチェックを外す。
4. 外した 1 件を再度チェックする。

期待値: 1 で全件が完了になり「残り 0 件」。2 で全件が未完了に戻る。3 で一括完了チェックが外れる。4 で全件完了になり一括完了チェックが再び入る。

## TC-06 編集モード（Editing）

1. 「郵便を出す」ラベルをダブルクリック。
2. 表示された入力の内容を確認する。

期待値: 該当行が編集モード（`li.editing`）になり、チェックボックス・削除ボタンが隠れ、既存タイトルが入った入力欄にフォーカスが当たる。

## TC-07 編集の保存（Enter / blur）

1. 「郵便を出す」を編集モードにして「郵便を出す（速達）」に変更し Enter。
2. 別の行を編集モードにしてタイトルを変更し、リスト外をクリックして blur。

期待値: いずれも変更後のタイトルで保存され、編集モードが解除される（`editing` クラスが外れる）。件数は変わらない。

## TC-08 編集内容が空なら削除される

1. 任意の行を編集モードにし、入力を全消去（または空白のみ）にして Enter。

期待値: その todo が削除され、リストから消える。残件数が更新される。

## TC-09 Escape で編集を取り消す

1. 任意の行を編集モードにし、タイトルを書き換えて Escape を押す。

期待値: 編集モードが解除され、タイトルは変更前のまま（変更が破棄される）。

## TC-10 個別削除（hover の × ボタン）

1. 任意の行にマウスを重ねる。
2. 表示された × をクリック。

期待値: 1 で削除ボタンが表示される。2 で該当 todo のみ削除され、残件数が更新される。

## TC-11 残件数の表示（Counter）

1. 2 件未完了の状態にする。
2. 1 件完了にする。
3. すべて完了にする。

期待値: 「残り 2 件」→「残り 1 件」→「残り 0 件」と数値のみが変化する（数値は `<strong>` で囲まれている）。

## TC-12 完了したタスクを削除（Clear completed）

1. 完了が 0 件の状態でボタンの有無を確認。
2. 2 件を完了にしてボタンをクリック。

期待値: 1 ではボタンが非表示。2 で完了済み 2 件のみが削除され、未完了は残る。削除後はボタンが非表示になり、一括完了チェックも外れた状態になる。

## TC-13 フィルター（Routing）

1. 未完了 2 件・完了 1 件の状態を作る。
2. 「未完了」をクリック。
3. 「完了」をクリック。
4. 「すべて」をクリック。
5. アドレスバーに直接 `http://localhost:7002/#/completed` を入力して開く（※データが消えるため、このケースは最後に単独で実施し、データを作り直してから確認する）。

期待値: 2 で URL が `#/active`、未完了 2 件のみ表示、「未完了」に選択枠が付く。3 で URL が `#/completed`、完了 1 件のみ表示。4 で URL が `#/` に戻り全 3 件表示。5 でハッシュ指定のとおり「完了」フィルターが選択される。

## TC-14 フィルター中に状態を変えると表示が更新される

1. 「未完了」フィルターを選択する。
2. 表示中の todo を 1 件完了にする。
3. 「完了」フィルターに切り替える。

期待値: 2 で完了にした todo が一覧から消え、残件数が減る。3 でその todo が完了一覧に表示される。

## TC-15 再読み込みでデータが消える（メモリー内保存の既知挙動）

1. 数件の todo を追加する。
2. ページを再読み込みする。

期待値: すべての todo が消え、TC-01 と同じ空状態になる（`localStorage` 永続化ではなくメモリー内保存のため）。app-spec.md の「Persistence」要件とは差異があることを既知の仕様差として記録する。
