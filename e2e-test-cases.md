# TodoMVC E2E テストケース (app-spec.md 準拠)

対象アプリ: `examples/javascript-es6`（UI は日本語）

## 前提・起動手順

1. `cd examples/javascript-es6`
2. `npm install`（未インストールの場合）
3. `npm run build && npm run serve`
4. ブラウザーで `http://localhost:7002` を開く

注意: このアプリの `Store` はメモリー内保存のため、リロードするとデータが消える。TC-11 以外ではシナリオ中にリロードしない。

UI 対応表（日本語 UI ↔ 仕様）:

| 仕様 | 画面表示 |
| --- | --- |
| New todo input | 「何をする必要がありますか？」 |
| Mark all as complete | 入力欄左の矢印 / 「すべて完了にする」 |
| Counter | 「残り N 件」 |
| Filters | 「すべて」「未完了」「完了」 |
| Clear completed | 「完了したタスクを削除」 |

---

## TC-01 初期表示（todo が 0 件）

- 手順: アプリを開く。
- 期待:
  - `#main`（`.main`）と `#footer`（`.footer`）が非表示。
  - 新規入力欄にフォーカスが当たっている（autofocus）。

## TC-02 新規 todo の作成

- 手順: 入力欄に「牛乳を買う」と入力し Enter。続けて「犬の散歩」「本を読む」を追加。
- 期待:
  - 3 件が追加順（上から 牛乳を買う → 犬の散歩 → 本を読む）に表示される。
  - 追加のたびに入力欄がクリアされる。
  - `.main` と `.footer` が表示される。
  - カウンターが「残り 3 件」。

## TC-03 空白のみの入力は作成されない

- 手順: 入力欄に半角スペースのみを入力し Enter。
- 期待: todo は増えない（件数は TC-02 のまま）。

## TC-04 前後空白のトリム

- 手順: 「　 買い物 　」のように前後に空白を付けて入力し Enter。
- 期待: 「買い物」としてトリムされて登録される。
- 後片付け: 追加した「買い物」を削除して 3 件に戻す。

## TC-05 個別の完了トグル

- 手順: 「犬の散歩」のチェックボックスをクリック。
- 期待:
  - 該当 `<li>` に `completed` クラスが付き、取り消し線が表示される。
  - カウンターが「残り 2 件」。
  - 「完了したタスクを削除」ボタンが表示される。
- 追加手順: もう一度クリックして解除。
- 期待: 取り消し線が消え、カウンターが「残り 3 件」に戻る。

## TC-06 カウンターの単複表記

- 手順: 3 件中 2 件を完了にし、さらに残り 1 件も完了にする。
- 期待: 「残り 1 件」→「残り 0 件」と正しく更新される（数値は `<strong>` で囲まれる）。
- 後片付け: すべて未完了に戻す。

## TC-07 すべて完了にする（Mark all as complete）

- 手順: 入力欄左のトグル（すべて完了にする）をクリック。
- 期待: 全 todo が完了になり、カウンターは「残り 0 件」。
- 追加手順: もう一度クリック。
- 期待: 全 todo が未完了に戻り、カウンターは「残り 3 件」。
- 追加手順: 1 件だけ手動で未完了にする。
- 期待: トグルのチェック状態が外れる。逆に全件を手動で完了にすると、トグルもチェック状態になる。

## TC-08 編集（保存・破棄・空文字）

- 手順 a: 「本を読む」のラベルをダブルクリック。
- 期待 a: `<li>` に `editing` クラスが付き、他のコントロールが隠れ、現在のタイトルが入った入力にフォーカスが当たる。
- 手順 b: 「本を読む（改題）」に変更して Enter。
- 期待 b: タイトルが更新され、`editing` クラスが外れる。
- 手順 c: 再度ダブルクリックして文字を変更し、フォーカスを外す（blur）。
- 期待 c: 変更が保存される。
- 手順 d: 再度ダブルクリックして文字を変更し、Escape を押す。
- 期待 d: 変更が破棄され、元のタイトルのまま編集モードが解除される。
- 手順 e: 再度ダブルクリックして空文字（または空白のみ）にして Enter。
- 期待 e: その todo が削除される。

## TC-09 削除ボタン（.destroy）

- 手順: todo 行にマウスを重ね、表示された × をクリック。
- 期待: ホバー時のみ × が表示され、クリックで該当 todo が消える。カウンターが減る。

## TC-10 フィルター（ルーティング）

- 手順: 未完了 1 件・完了 1 件以上の状態を作り、「すべて」「未完了」「完了」を順にクリック。
- 期待:
  - URL がそれぞれ `#/`、`#/active`、`#/completed` になる。
  - 表示される todo が絞り込まれる。
  - 選択中のフィルターリンクに `selected` クラス（枠線）が付く。
- 追加手順: `#/active` 表示中に、表示されている todo を完了にする。
- 期待: その todo が一覧から即座に消える。

## TC-11 永続化（app-spec.md の要求、既知の非準拠）

- 手順: todo をいくつか登録した状態でページをリロードする。
- 期待（仕様）: `todos-[framework]` キーで localStorage に保存され、リロード後も todo と選択中フィルターが復元される。
- 現状: 本実装の `src/store.js` はメモリー内保存のため、リロードで全 todo が消える想定。仕様との差分として記録する。
- 注意: 本ケースは必ず他ケースの後に最後に実施する。

## TC-12 「完了したタスクを削除」

- 手順: 完了 todo が存在する状態で「完了したタスクを削除」をクリック。
- 期待:
  - 完了 todo のみが削除され、未完了 todo は残る。
  - 完了 todo が 0 件になるとボタンが非表示になる。
  - 「すべて完了にする」トグルのチェック状態がクリアされる。

## TC-13 全件削除後の表示

- 手順: すべての todo を削除する。
- 期待: `.main` と `.footer` が再び非表示になる（TC-01 と同じ状態）。
